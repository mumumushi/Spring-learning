spring:
   application:
      name: config-server
   cloud:
      config:
         server:
            git:
               uri: https://github.com/mumumushi/cloud-config-repo-demo/
               username: mumumushi
               password: xxxxxxx
               #把密码注释掉
             
server:
   port: 8089
   
eureka:
   client:
      serviceUrl:
         defaultZone: http://localhost:8888/eureka
